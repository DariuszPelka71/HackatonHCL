

@{
    ViewBag.Title = "Log In";
}

<h2>Favorite Accounts</h2>


@using (Html.BeginForm("Validate", "Customer", FormMethod.Post, new { id = "frmCustomer" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)


    <div class="form-horizontal">
        <h4>Log In</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @* wait for model and added here *@
        @*<div class="form-group">
            @Html.LabelFor(model => model.Customer.Id, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Customer.Id, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Customer.Id, "", new { @class = "text-danger" })
            </div>
        </div>*@

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="LogIn" class="btn btn-primary" />
            </div>
        </div>

    </div>
}